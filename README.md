# 📈 주식 모니터 앱

실시간 주식 시세를 확인하고 가격 알림을 받을 수 있는 개인용 PWA(Progressive Web App)입니다.

## 주요 기능

- ✅ 실시간 주식 시세 조회 (Yahoo Finance API)
- ✅ 1분마다 자동 업데이트
- ✅ 현재 가격 실시간 표시
- ✅ PWA 지원 (홈 화면에 추가 가능)
- ✅ 모바일 친화적 반응형 디자인

## 시작하기

### 1. 의존성 설치

```bash
npm install
```

### 2. API 설정

이 앱은 **Yahoo Finance API**를 사용합니다. **API 키가 필요 없습니다!** 🎉

Yahoo Finance의 공개 API를 통해 실시간 주식 시세를 제공하며, CORS 프록시를 통해 브라우저에서 직접 호출합니다.

### 3. 개발 서버 실행

```bash
npm run dev
```

브라우저에서 `http://localhost:5173`으로 접속합니다.

### 4. 프로덕션 빌드

```bash
npm run build
```

빌드된 파일은 `dist` 폴더에 생성됩니다.

## 사용 방법

### 1. 주식 심볼 입력

주식 심볼을 입력하세요 (예: `AAPL`, `TSLA`, `MSFT`, `GOOGL` 등).

### 2. 자동 업데이트

앱은 1분마다 자동으로 주식 시세를 업데이트합니다. 수동으로 새로고침 버튼을 눌러 즉시 업데이트할 수도 있습니다.

### 3. 모바일 홈 화면에 추가

#### iOS (Safari)

1. Safari로 앱을 엽니다
2. 공유 버튼 (⬆️)을 누릅니다
3. "홈 화면에 추가"를 선택합니다

#### Android (Chrome)

1. Chrome으로 앱을 엽니다
2. 메뉴 (⋮)를 누릅니다
3. "홈 화면에 추가" 또는 "앱 설치"를 선택합니다

## 지원하는 주식

- **미국 주식**: AAPL, TSLA, MSFT, GOOGL, AMZN, RKLB 등 모든 미국 주식
- Yahoo Finance에서 지원하는 모든 주식 심볼 사용 가능

## API 정보

- **Yahoo Finance API** 사용
- **API 키 불필요** - 무료로 사용 가능
- CORS 프록시를 통해 안전하게 데이터 수신
- 실시간 주식 시세 데이터 제공

## 기술 스택

- **React** - UI 라이브러리
- **Vite** - 빌드 도구
- **Vite PWA Plugin** - PWA 기능 지원
- **Yahoo Finance API** - 주식 데이터 제공 (API 키 불필요)

## 라이선스

개인 사용 목적으로 자유롭게 사용하실 수 있습니다.

## 문제 해결

### 알림이 작동하지 않을 때

- HTTPS 환경에서 실행해야 합니다 (로컬호스트는 예외)
- 브라우저 설정에서 알림 권한을 확인하세요
- 모바일에서는 PWA로 설치된 후 사용해야 합니다

### API 오류가 발생할 때

- 인터넷 연결을 확인하세요
- 주식 심볼이 올바른지 확인하세요 (예: RKLB, AAPL, TSLA 등)
- CORS 프록시 서버가 일시적으로 다운되었을 수 있습니다. 잠시 후 다시 시도해주세요
