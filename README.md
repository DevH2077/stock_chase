# 📈 주식 모니터 앱

실시간 주식 시세를 확인하고 가격 알림을 받을 수 있는 개인용 PWA(Progressive Web App)입니다.

## 주요 기능

- ✅ 실시간 주식 시세 조회
- ✅ 모바일 알림 지원
- ✅ 가격 알림 설정 (특정 가격 도달 시 알림)
- ✅ PWA 지원 (홈 화면에 추가 가능)
- ✅ 모바일 친화적 반응형 디자인

## 시작하기

### 1. 의존성 설치

```bash
npm install
```

### 2. API 키 설정

이 앱은 [Alpha Vantage](https://www.alphavantage.co/) API를 사용합니다.

1. [Alpha Vantage API 키 발급 페이지](https://www.alphavantage.co/support/#api-key)에서 무료 API 키를 발급받습니다.
2. `src/App.jsx` 파일을 열고 다음 줄을 찾습니다:
   ```javascript
   const API_KEY = "demo"; // 실제 사용 시 환경변수로 관리하세요
   ```
3. `'demo'`를 발급받은 API 키로 변경합니다.

**또는 환경변수 사용 (권장):**

1. 프로젝트 루트에 `.env` 파일을 생성합니다.
2. 다음 내용을 추가합니다:
   ```
   VITE_ALPHA_VANTAGE_API_KEY=여기에_API_키_입력
   ```
3. `src/App.jsx`에서 `API_KEY`를 다음과 같이 수정합니다:
   ```javascript
   const API_KEY = import.meta.env.VITE_ALPHA_VANTAGE_API_KEY || "demo";
   ```

### 3. 개발 서버 실행

```bash
npm run dev
```

브라우저에서 `http://localhost:5173`으로 접속합니다.

### 4. 프로덕션 빌드

```bash
npm run build
```

빌드된 파일은 `dist` 폴더에 생성됩니다.

## 사용 방법

### 1. 주식 심볼 입력

주식 심볼을 입력하세요 (예: `AAPL`, `TSLA`, `MSFT`, `GOOGL` 등).

### 2. 알림 활성화

1. "🔔 알림 활성화" 버튼을 클릭합니다.
2. 브라우저에서 알림 권한 허용을 선택합니다.
3. 모바일에서도 알림을 받으려면 HTTPS 환경에서 사용해야 합니다.

### 3. 가격 알림 설정

- 알림 타입 선택: "이상" 또는 "이하"
- 목표 가격 입력
- 해당 가격에 도달하면 자동으로 알림이 울립니다

### 4. 모바일 홈 화면에 추가

#### iOS (Safari)

1. Safari로 앱을 엽니다
2. 공유 버튼 (⬆️)을 누릅니다
3. "홈 화면에 추가"를 선택합니다

#### Android (Chrome)

1. Chrome으로 앱을 엽니다
2. 메뉴 (⋮)를 누릅니다
3. "홈 화면에 추가" 또는 "앱 설치"를 선택합니다

## 지원하는 주식

- 미국 주식: AAPL, TSLA, MSFT, GOOGL, AMZN 등
- 한국 주식을 사용하려면 다른 API로 변경이 필요합니다 (예: 한국투자증권 API)

## API 제한사항

Alpha Vantage 무료 플랜의 경우:

- 분당 5회 호출 제한
- 하루 500회 호출 제한

앱은 1분마다 자동으로 업데이트하므로, 제한 내에서 사용 가능합니다.

## 기술 스택

- **React** - UI 라이브러리
- **Vite** - 빌드 도구
- **Vite PWA Plugin** - PWA 기능 지원
- **Alpha Vantage API** - 주식 데이터 제공
- **Web Push API** - 알림 기능

## 라이선스

개인 사용 목적으로 자유롭게 사용하실 수 있습니다.

## 문제 해결

### 알림이 작동하지 않을 때

- HTTPS 환경에서 실행해야 합니다 (로컬호스트는 예외)
- 브라우저 설정에서 알림 권한을 확인하세요
- 모바일에서는 PWA로 설치된 후 사용해야 합니다

### API 오류가 발생할 때

- API 키가 올바르게 설정되었는지 확인하세요
- API 호출 제한에 도달했는지 확인하세요 (무료 플랜 제한)
- 인터넷 연결을 확인하세요

### 한국 주식 데이터를 사용하고 싶을 때

현재는 Alpha Vantage API를 사용하므로 미국 주식만 지원합니다. 한국 주식을 사용하려면:

- 한국투자증권 API
- 네이버/다음 금융 크롤링 (개인 사용)
- Yahoo Finance API

등으로 변경이 필요합니다.
